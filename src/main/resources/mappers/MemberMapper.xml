<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="program.member.mapper.MemberMapper">
    <insert id="memberinsert" parameterType="cMap">
    	/* memberinsert :: 회원 추가 */
    	INSERT INTO member(USER_ID, USER_PW, NICK_NAME, EMAIL, GENDER, REG_DT)
    	VALUES (#{userId}, #{userPw}, #{nickName}, #{email}, #{gender}, DATE_FORMAT(REG_DT, '%Y-%m-%d'));
    </insert>
</mapper>